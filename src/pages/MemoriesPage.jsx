import "./MemoriesPage.css";

const memoryFiles = Array.from({ length: 276 }, (_, index) => `foto${index + 1}.webp`);

export function MemoriesPage({ activePage }) {
  return <div id="galeri" className={`page${activePage === "galeri" ? " active" : ""}`}><section className="memory-section"><div className="memory-hero"><div className="memory-content"><span className="memory-eyebrow">✦ KUMPULAN KENANGAN YVRIDIO'09 ✦</span><h2>Satu kelas,<br /><span>satu perjalanan.</span></h2><p className="memory-subtitle">Setiap foto menyimpan momen.<br />Setiap momen punya cerita.</p><div className="memory-divider"><span>✦</span><div /><span>✦</span></div><p className="memory-description">Kumpulan kecil dari perjalanan kami, tawa yang pernah memenuhi satu kelas, dan momen sederhana yang akhirnya menjadi kenangan.</p><div className="memory-counter"><div><strong id="memoryCount">0</strong><span>KENANGAN</span></div><div className="counter-line" /><div><strong>YVRIDIO'09</strong><span>SATU PERJALANAN</span></div></div></div></div><div className="gallery-heading"><div><span>✦ KUMPULAN KENANGAN</span><h3>Potongan cerita kita</h3></div><div className="gallery-actions"><button type="button" className="to-favorit-btn" onClick={() => window.showPage?.("favorit")}><span className="to-favorit-icon">✦</span><span className="to-favorit-text"><small>TERSIMPAN</small><strong>Ke Favorit</strong></span></button><button type="button" className="shuffle-memory-btn" onClick={() => window.shuffleMemories?.()}><span className="shuffle-orbit"><span className="shuffle-core" /><span className="shuffle-sat" /></span><span className="shuffle-text"><small>ACAK LAGI</small><strong>Acak Kenangan</strong></span></button></div></div><div id="memoryGallery" className="galeri">{memoryFiles.map((file, index) => <div className="memory-card" key={file} data-index={index}><img src={`/assets/images/kenangan/${file}`} alt={`Kenangan ${index + 1}`} loading="lazy" onClick={(event) => window.openModal?.(event.currentTarget.src, "memory")} /></div>)}</div><div className="memory-load-more-wrap" id="memoryLoadMoreWrap"><button type="button" className="memory-load-more-btn" id="memoryLoadMoreBtn" onClick={() => window.loadMoreMemories?.()}><span className="load-more-label"><small>TAMPILKAN LAGI</small><strong>Muat kenangan lain</strong></span><span className="load-more-count" id="memoryLoadMoreCount" /></button><p className="memory-load-status" id="memoryLoadStatus" /></div></section></div>;
}